# Architecture

The maintained architecture guide lives in [`docs/project/architecture.md`](docs/project/architecture.md).

This root pointer is kept for compatibility with existing repository checks and external links.
