# First reviewed product KPI baseline

This page records the first source-backed KPI dataset generated by the reviewed product KPI evaluator.

## Source

- repository: `pallets/click`
- target commit: `679a7a0eccbdded7a6e85680bdaaf08003765e01`
- review basis: merged PRs `#1899` and `#1900`
- source record: `docs/evidence/adoption-product-kpi/pallets-click-679a7a0-reviewed.json`
- KPI records: `docs/evidence/adoption-product-kpi/reviewed-kpi-records.v1.json`
- source-record SHA-256: `286cbc5d0a1ab849ccdc82e8e8f6aab209bce5a3b7f75c295a9e924aacfa61e0`

The target repository was not revisited for this review. The retained evidence records no dependency installation, no target-test execution, no target mutation, and no endorsement claim.

## Results

| Metric | Outcome |
| --- | --- |
| Discovery precision | `pass` |
| Proof-command actionability | `pass` |
| Authority-boundary preservation | `pass` |
| Authority rejection | `pass` |
| Operator actionability | `pass` |
| First-failure extraction precision | `not_applicable` |
| Workspace ownership precision | `not_applicable` |

The applicable denominator is one for five metrics. The two unexercised metrics keep a null precision and are not converted into passes or zeroes.

## Generate and verify

```bash
python -m sdetkit.adoption_product_kpi_report \
  --observations-json docs/evidence/adoption-product-kpi/reviewed-kpi-records.v1.json \
  --out build/sdetkit/adoption-product-kpi-report.json \
  --format text

python -m sdetkit.adoption_product_kpi_report \
  --observations-json docs/evidence/adoption-product-kpi/reviewed-kpi-records.v1.json \
  --out build/sdetkit/adoption-product-kpi-report.json \
  --check-freshness \
  --format text
```

The report command verifies that each repository-relative source file exists and matches its declared SHA-256 before generating or validating the report.

The existing `Adoption real-repo canonical replay` workflow publishes the JSON report, Markdown report, KPI records, and exact source record in one artifact bundle.
