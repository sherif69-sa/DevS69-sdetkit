# Azure DevOps proof discovery non-goals

This slice does not execute commands, expand templates, resolve variables, interpret tasks, evaluate matrices, access service connections, or mutate Azure DevOps resources.
