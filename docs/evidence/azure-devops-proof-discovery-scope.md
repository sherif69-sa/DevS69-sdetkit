# Azure DevOps proof discovery scope

This slice is read-only and repository-local. It recognizes root pipeline files and promotes only literal shell-family script steps to proof recommendations. All dynamic and external behavior remains review-first.
