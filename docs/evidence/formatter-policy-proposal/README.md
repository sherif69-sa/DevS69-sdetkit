# Formatter policy proposal evidence

This directory retains reviewed formatter policy proposal observation sources.

`reviewed-observations.v1.json` intentionally starts with an empty observation list. A record may be added only after a real formatter policy proposal is reviewed and its exact proposal artifact is retained at a repository-owned path with a matching SHA-256 digest.

Synthetic test fixtures must not be copied into this evidence directory.
