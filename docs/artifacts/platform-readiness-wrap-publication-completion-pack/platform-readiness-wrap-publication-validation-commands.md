#  validation commands

```bash
python -m sdetkit phase3-wrap-publication-completion --format json --strict
python -m sdetkit phase3-wrap-publication-completion --emit-pack-dir docs/artifacts/phase3-wrap-publication-completion-pack --format json --strict
python scripts/check_phase3_wrap_publication_completion_contract.py --skip-evidence
```
